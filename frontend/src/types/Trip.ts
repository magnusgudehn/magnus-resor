export interface Trip {
  id: string;
  destination: string;
  userId: string;
  startDate: string;
  endDate: string;
  description: string;
  imageUrl?: string;
  createdAt: string;
  updatedAt: string;
} 